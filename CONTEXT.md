# Vocabu

辅助背英语单词的桌面应用（MVP）。本文件是领域术语表——只收这个语境里特有的概念，定义必须精确到可判定。

## Language

### 词条

**单词（Word）**:
不含空白字符的英文词条，如 `apple`、`mother-in-law`。播报默认走「读音 + 字母拼写」路线。
_Avoid_: 生词（那是学习状态，不是词条类型）

**词组（Phrase）**:
由空白字符分隔多个英文词构成的词条，如 `look forward to`。判定规则：文本 trim 后含空白字符即为词组，导入/添加/编辑保存时自动计算，用户不可直接设置。播报默认走「读音 + 中文翻译」路线。

**词库（Wordbook）**:
用户全部词条的集合。MVP 中全局唯一，不做多词库。

### 学习状态

**考核面（Facet）**:
一对「刺激→反应」构成的考核维度，共三个：**英→中**（认读：英文文本→中文释义）、**中→英**（产出：中文释义→英文拼写）、**听→拼**（听写：英文音频→英文拼写）。每个考核面有独立的 SM-2 调度账本。
_Avoid_: 方向（Direction）——两账本时代的旧称

**学习记录（LearningRecord）**:
一个词条在**一个考核面**上的记忆状态：SM-2 参数（EF、连续成功次数、当前间隔）与下次复习时间。创建于该考核面首次被评级；随每次评级更新。一个词条最多三条记录（每考核面一条）。

**新词（New Word）**:
尚无任何考核面学习记录的词条。首次评级即告别新词身份；浏览（初步记忆）不改变身份。
_Avoid_: 生词

**复习词（Review Word）**:
学习记录中「下次复习时间」已到的词条。

**今日词表（Today's List）**:
某次学习使用的词条集合 = 新词（按添加顺序取每日新词数上限）+ 到期复习词（任一面到期）+ 缺面补齐词（配额内最旧活动优先）。在用户点击「开始学习」时生成。
_Avoid_: 任务清单、每日计划
